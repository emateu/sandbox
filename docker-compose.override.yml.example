# Copy to docker-compose.override.yml (gitignored); merged automatically.
# For host-specific mounts, e.g. a full custom user .npmrc:
services:
  sandbox:
    volumes:
      - ${HOME}/.npmrc:/home/${USERNAME}/.npmrc:ro,z
