# Copy to docker-compose.override.yml (gitignored); merged automatically.
# For host-specific mounts, e.g. targets of symlinked skills.
services:
  sandbox:
    volumes:
      - ${HOME}/Code:/var/lib/claude-ephemeral/Code:ro,z
